{
  "_comment": "Default rewrite rule config. All rules enabled by default.",
  "rules": {
    "account_id": {
      "enabled": true,
      "description": "Replace hardcoded source account ID with !Sub '${AWS::AccountId}'"
    },
    "region": {
      "enabled": true,
      "description": "Replace hardcoded source region with !Sub '${AWS::Region}'"
    },
    "ami_id": {
      "enabled": true,
      "description": "Replace hardcoded AMI ID with !Ref AmiId parameter (SSM public path default)",
      "ssm_default_path": "/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64"
    },
    "availability_zone": {
      "enabled": true,
      "description": "Replace hardcoded AZ with !Select [0, !GetAZs '']"
    },
    "deletion_policy": {
      "enabled": true,
      "description": "Add DeletionPolicy: Retain to RDS / S3 / DynamoDB / EFS resources",
      "retain_types": [
        "AWS::RDS::DBInstance",
        "AWS::RDS::DBCluster",
        "AWS::S3::Bucket",
        "AWS::DynamoDB::Table",
        "AWS::EFS::FileSystem"
      ]
    }
  }
}
